# Spec (draft)

- Generated: 2025-12-21 16:57:42
- Repo: `/Users/s-kusaba/projects/code2spec-agent`
- Query: `argparse`

## Overview
This document is automatically generated from the repository contents.
It lists candidate important files and evidence snippets found by keyword search.

## Candidate important files (first 50)
- `.env.example`
- `README.md`
- `app/__init__.py`
- `app/ingest.py`
- `app/llm.py`
- `app/main.py`
- `app/render.py`
- `app/retrieve.py`
- `app/summarize.py`
- `requirements.txt`

## LLM summary (draft)
This section is generated by an LLM from the evidence snippets below.

## Summary
このリポジトリは、`argparse`モジュールを使用してコマンドライン引数を解析し、指定されたリポジトリから仕様書を生成するPythonアプリケーションです。生成された仕様書はLLMによる要約を含むことがあります。

## What it can do
- コマンドライン引数を解析する (app/main.py:13)
- ターゲットとなるローカルリポジトリのパスを受け取る (--repo 引数) (app/main.py:14)
- 出力する仕様書のパスを指定する (--out 引数) (app/main.py:15)
- 検索クエリを受け取る (--query 引数) (README.md:31)
- LLM (大規模言語モデル) による要約を生成する (README.md:29, 31)
- 仕様書を生成する (README.md:29)

## Key components
- `app/main.py`: プログラムのエントリーポイントであり、`argparse` を利用してコマンドライン引数を処理する主要なスクリプトです。
    - `import argparse` (app/main.py:3)
    - `ap = argparse.ArgumentParser()` (app/main.py:13)
    - `ap.add_argument("--repo", ...)` (app/main.py:14)
    - `ap.add_argument("--out", ...)` (app/main.py:15)
- `README.md`: プログラムの実行方法と、LLM要約つきの生成機能について記述されています。
    - `3) 生成（LLM要約つき）` (README.md:29)
    - `python -m app.main ... --llm` (README.md:31)

## Notes / Limits
- `argparse` はコマンドライン引数解析のために使用されていますが、具体的な引数の値がどのように利用され、仕様書がどのように生成されるか（どの情報が抽出されるか）については、この証拠だけでは不明です。
- LLMが具体的にどのようなタスクを実行し、どのLLMが使用されているかについては不明です。
- `--context` 引数の具体的な目的や動作は不明です。
- `--query` 引数で指定された検索クエリが、仕様書生成プロセスの中でどのように利用されるかは不明です。


## Evidence (keyword hits)
Each hit includes file path, line number, and surrounding context.

### Hit 1
- File: `README.md`
- Line: `31`

```text
     29: 3) 生成（LLM要約つき）
     30: OUT="$(mktemp /tmp/spec.XXXXXX)"
>>   31: python -m app.main --repo . --query "argparse" --context 2 --out "$OUT" --llm
     32: mkdir -p docs
     33: cp "$OUT" docs/spec.md
```

### Hit 2
- File: `app/main.py`
- Line: `3`

```text
      1: from __future__ import annotations
      2: 
>>    3: import argparse
      4: from pathlib import Path
      5: 
```

### Hit 3
- File: `app/main.py`
- Line: `13`

```text
     11: 
     12: def main():
>>   13:     ap = argparse.ArgumentParser()
     14:     ap.add_argument("--repo", type=str, required=True, help="Target repo path (local)")
     15:     ap.add_argument("--out", type=str, default="docs/spec.md", help="Output spec path")
```
